{
  "name": "wellserve-payroll",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node server.js"
  }
}